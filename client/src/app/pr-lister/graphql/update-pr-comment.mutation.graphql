mutation updatePrComment($id: ID!, $comment: String!) {
  updatePrComment(id: $id, comment: $comment) {
    id
    project
    title
    description
    repository
    prId
    link
    date
    comment
    isReviewed
  }
}
